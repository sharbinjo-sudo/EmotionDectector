<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Gradient background (red → orange → yellow) -->
    <LinearLayout
        android:id="@+id/rootLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="20dp"
        android:gravity="center"
        android:background="@drawable/gradient_bg_red_orange_yellow">

        <!-- App Title -->
        <TextView
            android:id="@+id/titleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Emotion Detector"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#FFF9E1"
            android:layout_marginBottom="30dp"
            android:fontFamily="sans-serif-medium" />

        <!-- Camera Preview Box -->
        <FrameLayout
            android:layout_width="320dp"
            android:layout_height="320dp"
            android:background="@drawable/camera_preview_bg_glow"
            android:layout_marginBottom="30dp"
            android:elevation="10dp"
            android:clipToOutline="true"
            android:layout_gravity="center"
            android:padding="5dp">

            <androidx.camera.view.PreviewView
                android:id="@+id/previewView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                app:implementationMode="performance" />

            <com.emotion.detection.OverlayView
                android:id="@+id/overlayView"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </FrameLayout>

        <!-- Emotion -->
        <TextView
            android:id="@+id/emotionText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Emotion:"
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:layout_marginBottom="10dp"
            android:fontFamily="sans-serif-medium" />

        <!-- Confidence -->
        <TextView
            android:id="@+id/confidenceText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Confidence:"
            android:textColor="#FFE9B5"
            android:textSize="16sp"
            android:layout_marginBottom="20dp"
            android:fontFamily="sans-serif" />

        <!-- All Confidences -->
        <TextView
            android:id="@+id/allConfidences"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFF4CC"
            android:textSize="14sp"
            android:gravity="center"
            android:layout_marginBottom="20dp"
            android:fontFamily="monospace" />

        <!-- Start Detection Button -->
        <Button
            android:id="@+id/startButton"
            android:layout_width="220dp"
            android:layout_height="wrap_content"
            android:text="Start Detection"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:padding="12dp"
            android:layout_gravity="center"
            android:background="@drawable/button_rounded_warm"
            android:elevation="8dp"
            android:clipToOutline="true"
            android:stateListAnimator="@null"
            app:rippleColor="#40FFFFFF"
            android:fontFamily="sans-serif-medium" />

    </LinearLayout>
</RelativeLayout>
